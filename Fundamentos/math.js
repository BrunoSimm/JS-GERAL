const raio = 5.6
const area = Math.PI * Math.pow (raio, 2) 
/* Calcular área circunferencia
raio x pi ao quadrado, para elevar ao quadrado no JS deve-se usar o Object Math
No caso a cima usei Math.pow (var que quero elevar(raio), expoente que elevarei(2)) */
console.log(area.toFixed(2))

console.log(Math.sqrt(3).toFixed(2)) // Math.sqrt(x) tira a raiz quadrada de x

